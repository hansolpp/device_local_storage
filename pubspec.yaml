name: device_local_repository
description: device_local_repository
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.0-116.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Icons
  cupertino_icons: 1.0.4

  # Flutter downloader
  flutter_downloader: 1.7.0
  path_provider: ^2.0.7

  # Open file
  open_file: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true